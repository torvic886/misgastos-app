<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.ComboBox?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.TextArea?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.GridPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<VBox spacing="20" styleClass="content" xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.misgastos.controller.RegistroGastoController">
    <padding>
        <Insets top="30" right="30" bottom="30" left="30"/>
    </padding>
    
    <children>
        <Label text="📝 Registrar Nuevo Gasto" styleClass="content-title"/>
        <Label text="Complete el formulario para registrar un nuevo gasto" styleClass="content-subtitle"/>
        
        <GridPane hgap="15" vgap="15">
            <columnConstraints>
                <javafx.scene.layout.ColumnConstraints minWidth="150" prefWidth="150"/>
                <javafx.scene.layout.ColumnConstraints minWidth="300" prefWidth="400" hgrow="ALWAYS"/>
            </columnConstraints>
            
            <!-- Categoría -->
            <Label text="Categoría:" GridPane.columnIndex="0" GridPane.rowIndex="0" styleClass="form-label"/>
            <ComboBox fx:id="cmbCategoria" promptText="Seleccione categoría" maxWidth="Infinity" GridPane.columnIndex="1" GridPane.rowIndex="0"/>
            
            <!-- Subcategoría -->
            <Label text="Subcategoría:" GridPane.columnIndex="0" GridPane.rowIndex="1" styleClass="form-label"/>
            <ComboBox fx:id="cmbSubcategoria" promptText="Seleccione subcategoría" maxWidth="Infinity" GridPane.columnIndex="1" GridPane.rowIndex="1"/>
            
            <!-- Producto -->
            <Label text="Producto:" GridPane.columnIndex="0" GridPane.rowIndex="2" styleClass="form-label"/>
            <TextField fx:id="txtProducto" promptText="Nombre del producto o servicio" GridPane.columnIndex="1" GridPane.rowIndex="2"/>
            
            <!-- Cantidad -->
            <Label text="Cantidad:" GridPane.columnIndex="0" GridPane.rowIndex="3" styleClass="form-label"/>
            <TextField fx:id="txtCantidad" text="1" promptText="Cantidad" GridPane.columnIndex="1" GridPane.rowIndex="3"/>
            
            <!-- Valor Unitario -->
            <Label text="Valor Unitario:" GridPane.columnIndex="0" GridPane.rowIndex="4" styleClass="form-label"/>
            <TextField fx:id="txtValorUnitario" promptText="0.00" GridPane.columnIndex="1" GridPane.rowIndex="4"/>
            
            <!-- Valor Total -->
            <Label text="Valor Total:" GridPane.columnIndex="0" GridPane.rowIndex="5" styleClass="form-label"/>
            <TextField fx:id="txtValorTotal" text="0.00" editable="false" style="-fx-background-color: #e2e8f0;" GridPane.columnIndex="1" GridPane.rowIndex="5"/>
            
            <!-- Notas -->
            <Label text="Notas (opcional):" GridPane.columnIndex="0" GridPane.rowIndex="6" styleClass="form-label"/>
            <TextArea fx:id="txtNotas" promptText="Comentarios adicionales..." prefRowCount="3" GridPane.columnIndex="1" GridPane.rowIndex="6"/>
        </GridPane>
        
        <HBox spacing="15" alignment="CENTER_RIGHT">
            <Button text="Limpiar" onAction="#handleLimpiar" styleClass="secondary-button"/>
            <Button text="💾 Guardar Gasto" onAction="#handleGuardar" styleClass="primary-button"/>
        </HBox>
    </children>
</VBox>